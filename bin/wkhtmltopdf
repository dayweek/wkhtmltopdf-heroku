#!/usr/bin/env ruby

arch = case RUBY_PLATFORM
  when /64.*linux/
    'amd64'
  when /darwin/
    'i368'
  else
    raise "Invalid platform. Must be running linux or intel-based Mac OS."
end

args = $*.map { |x| x.include?(' ') ? "'" + x + "'" : x }
cmd = File.expand_path "#{File.dirname(__FILE__)}/wkhtmltopdf-#{arch}"

exec "#{cmd} #{args.join(' ')}"